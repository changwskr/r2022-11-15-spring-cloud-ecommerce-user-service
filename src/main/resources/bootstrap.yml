spring:
  cloud:
    config:
      #--
      # config-service의 정보를 입력한다.
      uri: http://127.0.0.1:8888
      # 2022-11-28-2 암복호화 대칭으로 변경
      # config-service에서 어떤 파일을 읽어야 될지 결정한다.
      name: user-service

# 2022-11-28-1 spring-bus 적용으로 사용을 안한다
#  profiles:
#    active: dev
